/*
  DOM是一套独立的标准，网络文档的标准
    不仅适用于HTML，也适用于其他文档，比如XML、SVG、MXML...

  DOM就是将文档转换为对象的一套标准
  官方术语：规范文档跟对象的转换方式。

  ES根据DOM的这套标准，就可以操作DOM文档了。



  1.DOM标准的组成（DOM标准的结构 ）
    DOM1
        ---只有一个标准（内容包括Core和HTML）
    DOM2
        ---分为多个子标准（Events、Views、Style、Traversal and Range）
    DOM3
        ---validation 、Load and Save 

  2.DOM标准的特点
    共同特点：所有对象都以接口的形式来定义（异常对象除外）
    
    具体语言在实现DOM接口的时候，可以对接口起别名。
*/


