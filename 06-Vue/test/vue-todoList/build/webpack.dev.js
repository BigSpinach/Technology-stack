const path =require('path');
module.exports = {
  mode:'development',
  devServer:{
    port:8989,
    quiet:true,
    open:true
  },
  // devtool: 'cheap-module-eval-source-map',
  // devtool:'cheap-module-source-map'
}